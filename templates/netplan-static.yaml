# Generated by ubuntu-24-toolkit — script 03-ip-config
# Variables substituted via envsubst:
#   ${NETWORK_INTERFACE}, ${IP_ADDRESS}, ${PREFIX_LENGTH}, ${GATEWAY}, ${DNS_SERVERS_YAML}
network:
  version: 2
  renderer: networkd
  ethernets:
    ${NETWORK_INTERFACE}:
      dhcp4: false
      dhcp6: false
      accept-ra: false
      addresses:
        - ${IP_ADDRESS}/${PREFIX_LENGTH}
      routes:
        - to: default
          via: ${GATEWAY}
      nameservers:
        addresses:
${DNS_SERVERS_YAML}
