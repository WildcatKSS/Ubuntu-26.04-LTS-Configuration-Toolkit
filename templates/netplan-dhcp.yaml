# Generated by ubuntu-26-toolkit — script 02-ip-config
# Variables substituted via envsubst:
#   ${NETWORK_INTERFACE}
network:
  version: 2
  renderer: networkd
  ethernets:
    ${NETWORK_INTERFACE}:
      dhcp4: true
      dhcp6: false
      accept-ra: false
